/**
 * Default values for new alarms and app behavior.
 */

export const DEFAULT_SNOOZE_MINUTES = 5;
export const DEFAULT_VIBRATE = true;
export const DEFAULT_IS_ENABLED = true;
export const DEFAULT_REPEAT_DAYS = 0;
